[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "brainagent-kernel"
version = "0.1.0.dev0"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["../.."]
include = ["brain_kernel*"]
