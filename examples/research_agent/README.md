# Independent Research Agent

This consumer imports only `domain_sdk`. After installing the SDK and this example:

```bash
brainagent --database research.db \
  --plugin research_agent:ResearchAgentPlugin status
```

No Kernel, Platform, or quant application source is modified by this domain.
